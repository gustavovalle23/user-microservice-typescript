version: '3.5'

services:
  app:
    hostname: 'app-user-microservice'
    container_name: 'app-user-microservice'
    ports:
      - "4002:4000"
      - "9229:9229"
    volumes:
      - .:/app
    build:
      dockerfile: Dockerfile
      context: .
    networks:
      - local-network
    env_file:
      - .env
    extra_hosts:
      - 'host.docker.internal:172.17.0.1'
  mongo:
    ports:
      - "27047:27017"
    volumes:
      - 'mongodb-volume:/data/db'
    hostname: 'mongo-user-microservice'
    container_name: 'mongo-user-microservice'
    image: 'mongo:4.2.3'
    environment:
      MONGO_INITDB_ROOT_USERNAME: test
      MONGO_INITDB_ROOT_PASSWORD: test
    networks:
      - local-network
    networks:
      - local-network
networks:
  local-network:
    name: network-user-microservice
volumes:
  mongodb-volume:
    driver: local
